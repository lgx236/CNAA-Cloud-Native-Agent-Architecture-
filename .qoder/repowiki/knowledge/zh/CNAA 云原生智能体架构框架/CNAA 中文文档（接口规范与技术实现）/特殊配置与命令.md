文档中记录了项目运行命令：`python server.py` 启动 HTTP 服务（默认 localhost:8080），`python mcp_stdio_server.py` 启动 stdio 模式 MCP 服务，`python -m pytest tests/ -v` 运行测试，以及通过环境变量 `CNAA_AUTH_ENABLED`、`CNAA_API_KEYS`、`CNAA_ALLOW_UNAUTHENTICATED` 启用认证。