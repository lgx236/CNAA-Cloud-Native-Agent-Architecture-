- 配置与上下文对象统一使用 `@dataclass` 定义，并通过工厂函数（如 `load_auth_config_from_env`）从环境变量构建。
- 工具定义采用「名称常量 + 描述字符串 + inputSchema 引用」的三元组结构，集中由 `get_tool_definitions()` 返回列表。
- 权限控制通过 `PermissionLevel` 枚举与 `TOOL_PERMISSION_MAP` 将工具名映射到 "read"/"write" 两级，再由 `check_permission` 统一判定。
- 对外暴露的函数保持纯函数风格，不持有全局状态，便于测试与组合。