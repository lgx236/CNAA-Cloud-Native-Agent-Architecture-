- 每个 MCP 工具调用统一通过 `_call_mcp_tool` 方法封装，传入工具名与参数字典，避免重复 requests.post 逻辑。
- 方法命名采用动词+名词模式（store_memory、get_state、update_preference 等），与后端 MCP 工具名 cnaa_* 前缀一一对应。
- 所有公共方法使用 Google 风格 docstring，包含 Args/Returns 描述及典型使用场景注释。
- 可选参数统一使用 `| None` 联合类型与默认值（如 `tags: list[str] | None = None`），保持接口一致性。